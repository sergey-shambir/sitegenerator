# Генератор статического сайта, созданный для личных нужд

Основан на преобразовании Markdown→HTML и SCSS→CSS.

Сборка:

```bash
# Сборка проекта и docker-образа
make

# Сборка проекта без сборки docker-образа
make build
```
